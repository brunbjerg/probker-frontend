version: '3'
services:
  nginx:
    image: nginx:latest
    ports:
      - "8080:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - julia-app

  julia-app:
    build: .
    expose:
      - "8081"
